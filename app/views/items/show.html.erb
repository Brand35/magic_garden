<div class="card">
  <div class="card-header">
    <h1><%= @item.name %></h1>
  </div>
  <div class="card-body">
    <%= cl_image_tag @item.photo.key, height: 400, width: 400, crop: :fill, class: "rounded m-3"%>
    <p><strong>Description :</strong> <%= @item.description %></p>
    <p><strong>Prix :</strong> <%= number_to_currency(@item.price) %></p>
  </div>
  <div class="card-footer">
    <%= link_to "Retour à la liste", root_path, class: "btn btn-secondary" %>
    <%= link_to 'Louer cet objet', new_item_booking_path(@item), class: 'btn btn-primary' %>
  </div>
</div>
