<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Magic Garden" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <section>
      <div class='air air1'></div>
      <div class='air air2'></div>
      <div class='air air3'></div>
      <div class='air air4'></div>
    </section>
      <%= render "shared/navbar" %>
      <%= render "shared/flashes" %>
      <%= yield %>

  </body>

  <footer class="footer mt-5 py-3">
  <div class="text-center">
    <p class="mt-0"><p>
  Magic Garden a vu le jour grâce au batch #1825 <strong>Phantom GO</strong>,
  forgé dans les flammes de la créativité, les larmes de la persévérance et un peu trop de café.
</p>
</div>
</html>
